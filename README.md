# Clojre Core Async Example

Example project using Clojure core async so that I don't have to do it again.

## Usage
Intellij Idea file: lein idea

Clojure Script: lein cljsbuild auto

Start Server : lein ring server
